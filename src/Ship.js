export default function Ship() {
  const length = len;
  let isSunk = false;
  const xCord = x;
  const yCord = y;

  const hit = (x, y) => {
    return null;
  }

  const isSunk = () => {

  }

  return {
    xCord, yCord, hit, isSunk
  }
}
